version: '3'

services:
  retailpulse:
    image: retailpulse
    ports:
      - "8501:8501"

  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"